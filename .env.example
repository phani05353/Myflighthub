# Get a free key at https://aviationstack.com (500 req/month on free tier)
AVIATIONSTACK_API_KEY=your_key_here

# Port the Express server listens on (default: 3000)
PORT=3000

# Absolute path for the SQLite database (default: ./data/)
# DATA_DIR=/app/data
